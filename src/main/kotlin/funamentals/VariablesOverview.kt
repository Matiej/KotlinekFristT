package funamentals

class VariablesOverview {
    fun variablesListening(ageParam: Int) {
        val age: Int = ageParam;
        println(age)
        var ageToModify = 1
        if(age == 1) {
            ageToModify = 1
        } else {
            ageToModify=2
        }
    }

}